# H0.0 Result - Fork + Seam Smoke Test (2026-08-17)

Exit gate: Hyperloom launches workload, measures, terminates, persists a
result; production untouched. **PASSED.**

## SHAs

- Upstream pinned: `8b2a912c04216739e868675010e456c749d764e4` (Hyperloom main,
  fork time). Branch `feature/rdna-llamacpp` in
  `github.com/richardoros/Hyperloom` (fork).
- Fork commits: `79e672c75` (PLAN.md), `ad7f38101` (seam + gfx identity),
  `d7021190b` (timings fix + smoke driver). HEAD `d7021190b`.
- Local clone: `/home/homelabserver/hyperloom-rdna` (branch `feature/rdna-llamacpp`,
  pushed; upstream remote tracks AMD-AGI/Hyperloom).
- No llama.cpp code changed in H0.0 (seam only).

## Inventory findings (existing assets, nothing rebuilt)

| Path | What it is |
|---|---|
| `src/llama.cpp-turboquant/` | PRODUCTION checkout: richardoros/buun-llama-cpp fork, branch `perf/rocm-fused-gdn-exp2-swa-deferred-recurrent`, HEAD `c6903632b` (2026-08-14). Contains `build/` with the running binary, `codebooks/{3bit,2bit}/` TCQ codebooks. |
| `src/buun-llama-cpp/` | Second checkout of spiritbuun/buun-llama-cpp on `master` (upstream tracking). |
| `llama-turboquant/` | unixsysdev/llama-turboquant checkout (Apr 2026, `main`) with `build-tq3*`/`build-rocm`/`build-vulkan` trees. Not the production lineage. |
| `models/` | GGUFs incl. `Qwen3.8-27B-UD-Q5_K_XL.gguf` (19.3 GB), `Qwen3.6-27B-UD-Q5_K_XL.gguf`, embedding models. |
| `inference-bench/` | Harness moved out of apply (2026-08-15); subdirs mostly empty scaffolding; `turboquant-result.md` has a prior 131K turbo4 run record. |
| `benchmarking/` | LLM bench project (bench.toml, providers, uv.lock) - cloud-model benchmarking, not the llama.cpp harness. |
| `apply/` | JustApply repo - untouched. |

## Production map (recorded)

- Service `qwen38-turboquant.service` (system unit, enabled): currently
  INACTIVE, nothing on 18079. Port 18079, model Qwen3.8-27B-UD-Q5_K_XL.
- GGUF SHA-256: `176a6a3f034e9cdc447c10cd00329fc9b31002e6589b9295f2ad4f1eefe0f6ab`
- Binary: `src/llama.cpp-turboquant/build/bin/llama-server` (fork HEAD c6903632b)
- Flags: `-ngl 99 -c 131072 -np 1 -kvu -b 512 -ub 256 -fa on -ctk turbo4 -ctv
  turbo4 --cache-ram 0 -ctxcp 32 -cms 256 --fit off --no-logits-all`
- Env: `ROCR_VISIBLE_DEVICES=0 HIP_VISIBLE_DEVICES=0 GPU_DEVICE_ORDINAL=0
  CUDA_VISIBLE_DEVICES=0 HSA_ENABLE_SDMA=0 GPU_MAX_HEAP_SIZE=100
  GPU_MAX_ALLOC_PERCENT=100 GPU_MAX_HW_QUEUES=1 LD_LIBRARY_PATH=/opt/rocm/lib
  TURBO_TCQ_CB=<3bit cb> TURBO_TCQ_CB2=<2bit cb>`, Slice `justapply-ai.slice`.
- Prior comparable result (Qwen3.6, 131K ctx, turbo4): pp 285.9, tg 10.33 tok/s
  (inference-bench/turboquant-result.md) - NOTE: that run used `--no-cache-prompt`
  and different context; not directly comparable to this seam's 4K measurement.

## Seam wired (fork changes only)

- `src/hyperloom/common/gpu_identity.py`: added `gfx1100` (96 CU), `gfx1150`
  (16 CU) dispatch identities.
- `src/hyperloom/inference_optimizer/gpu_types.py`: `_GFX_TO_RUNNER` entries so
  `--gpu-type gfx1100` resolves and the runner label matches the script name.
- `rdna/bench/custom_gfx1100.sh`: bypass-scriptable entrypoint. Contract:
  launched as `bash custom_gfx1100.sh`; reads `MODEL`, `RESULT_DIR`,
  `RESULT_FILENAME`, `EXTRA_CUSTOM_ARGS`; writes flat InferenceX-shaped
  `inferencex_result.json` incl. required `quality_gate.passed`; server flags
  default to the production unit but bind port 18179; env matches the unit;
  `trap EXIT` kills the server. Reads buun-llama-cpp timings keys
  (`prompt_n`/`predicted_n`, NOT upstream `prompt_eval_count`/`eval_count`).
- `rdna/tools/h0_smoke_driver.py`: calls Hyperloom's own
  `_run_scriptable_benchmark` (the orchestrator's executor) with a
  baseline_custom-shaped bench dict.

## Commands run

1. `gh repo fork AMD-AGI/Hyperloom --clone` -> moved to hyperloom-rdna/,
   `git checkout 8b2a912c0` then `git checkout -b feature/rdna-llamacpp`.
2. `cp PLAN.md; git commit; git push` (identity set to richardoros <risoo5@hotmail.com>).
3. Standalone seam run (3 iterations while fixing key names):
   `MODEL=... bash rdna/bench/custom_gfx1100.sh` in /tmp/h0-run.
4. Hyperloom-executor run:
   `MODEL=... HYPERLOOM_BYPASS_SCRIPTS_DIR=rdna/bench USER_DATA_PATH=/tmp/h0-userdata
   python3 rdna/tools/h0_smoke_driver.py /tmp/h0-hyperloom`
5. `PYTHONPATH=src pytest test_custom_framework.py test_gpu_board_list_single_source.py
   test_gpu_pool_device_resolution.py` -> 58 passed.

## Persisted H0.0 result

Standalone (3rd, final): pp=705.7 tok/s, tg=25.7 tok/s, ttft=4785 ms,
prompt_n=3377, eval_n=128, quality_gate.passed=true -
`/tmp/h0-run/inferencex_result.json`, `/tmp/h0-run/server.log`.

Hyperloom-executor (authoritative): success=true, output_throughput=26.402,
total_token_throughput=368.422, ttft mean 4632 ms, tpot mean 37.9 ms,
e2el mean 4848 ms, quality_gate.passed=true, workload_kind=scriptable,
throughput_unit=tok/s -
`/tmp/h0-hyperloom/benchmark_custom_20260817_150904/benchmark_report.json`
(+ inferencex_result.json, config.yaml, scriptable logs in same workspace).

## Failures / workarounds

1. Fork `git config` identity missing -> set repo-local
   richardoros <risoo5@hotmail.com>; amended the PLAN.md commit (force-push
   with lease; branch was 2 min old, single author).
2. First seam run: `BENCH_FAIL: empty completion or zero eval tokens` - buun
   fork timings keys differ from upstream; server log showed real values
   (736 pp / 26.9 tg). Fixed key names, reran (746 pp / 26.7 tg), then fixed
   ttft semantics (prompt_ms, not per-token x1000): final 705.7 / 25.7.
3. Driver crashed: `output_dir` str vs Path in `create_workspace` - fixed by
   passing `Path` (Hyperloom bug-compat: callers pass Path, signature untyped).
4. `MODEL` env is required by the script; first driver-style run without it
   failed fast (by design, set -u).

## Verification / gate status

- Launch on 18179: confirmed (server.log, health poll, timings).
- Measure: confirmed (above numbers).
- Terminate: confirmed - no llama-server left on 18179 after either run;
  embedding servers on 8083/8084 (CPU) untouched.
- Persist: confirmed - inferencex_result.json + benchmark_report.json.
- :18079 untouched: confirmed - service inactive before/after, port never
  listened; no config/service changes anywhere.
- Capability preflight: all H0.0 requirements present (Fedora 43, git 2.54,
  gh 2.87 auth richardoros, python 3.14.4, cmake/ninja, rocm-smi/rocminfo,
  vulkaninfo, podman, sha256sum/curl/jq). claude + codex CLIs installed for
  later agent-phase use.

## Notes for H0.5

- The seam's measurement is ONE 4K prompt + 128 tokens. H0.5 owns the tiered
  matrix, repetition, variance, load gate, and the experiment DB.
- The `custom` scriptable path bypasses Magpie/InferenceX entirely; the
  normalized report is consumed by Hyperloom's own collectors unchanged.
- The evaluator (H0.5) must build llama.cpp from a pinned SHA; today's
  production HEAD is `c6903632b` (fork remote richardoros/buun-llama-cpp).
- No upstream model/token callbacks were made by H0.0: no agent loop ran,
  no LLM tokens spent.